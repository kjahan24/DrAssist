# Celery task modules are registered here. None are defined yet — add
# `@celery_app.task` functions in sibling modules as background jobs are
# introduced (e.g. transcription, OCR, embedding generation).
