# SQLAlchemy ORM models are registered here so Alembic autogenerate can
# discover them (import each model module in this file once it exists).
# No models are defined yet — see app/infrastructure/database/base.py for
# the declarative base and shared mixins new models should use.
