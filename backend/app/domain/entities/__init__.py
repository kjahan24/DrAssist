# Pure domain entities live here — plain Python objects with identity and
# invariants, framework-agnostic (no SQLAlchemy, no Pydantic). Nothing is
# defined yet; see base.py for the shared entity contract.
