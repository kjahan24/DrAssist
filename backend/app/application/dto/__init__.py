# Data Transfer Objects passed into/out of use cases. Kept distinct from
# app/schemas (API request/response models) so the application layer never
# depends on FastAPI/Pydantic wire formats. None are defined yet.
