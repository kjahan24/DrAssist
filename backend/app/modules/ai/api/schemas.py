"""Pydantic API schemas for the AI module.

Empty for this task — no endpoints exist yet (`router.py`'s own
docstring). A future AI-feature module adds request/response schemas here
following `app.schemas.base.ORJSONModel`, the same convention every other
module's `api/schemas.py` already follows.
"""
