"""Pydantic API schemas for the AI Clinical Copilot module.

Empty for this task — `router.py` exposes a placeholder health route
only, per this task's "Create placeholder endpoints only. No medical
intelligence yet." A future clinical-feature module (or a later task
scoped to this module's own real HTTP surface) adds
`<Noun>Request`/`<Noun>Response` schemas here following
`app.schemas.base.ORJSONModel`, the same convention every other module's
`api/schemas.py` already follows.
"""
