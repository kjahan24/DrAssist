"""Pydantic API schemas for the AI SOAP Note Generation module.

Empty for this task — `router.py` exposes a placeholder health route
only. A future consumer module adds request/response schemas here,
following `app.schemas.base.ORJSONModel`, the same convention every other
module's own API schemas follow.
"""
