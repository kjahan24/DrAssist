"""Pydantic request/response schemas for the AI Healthcare Orchestrator
module's HTTP surface.

Empty for now — `presentation/router.py` exposes only a placeholder
health route (no request/response bodies yet), the same "structure-only,
placeholder-endpoints-only" scope every prior AI module's own
`presentation/schemas.py` establishes for itself.
"""
