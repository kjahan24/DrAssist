# Core runtime dependencies shared by all environments.
# Pin exact versions before production release; ranges below are
# starting points for a new project scaffold.

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0

# --- Settings & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10

# --- Cache ---
redis==5.2.1

# --- Vector database ---
qdrant-client==1.12.1

# --- Task queue ---
celery[redis]==5.4.0
flower==2.0.1

# --- Object storage ---
minio==7.2.13

# --- AI / ML ---
google-generativeai==0.8.3
faster-whisper==1.1.0
paddleocr==2.9.1
paddlepaddle==2.6.2

# --- Auth / security primitives ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (last released 2020) reads bcrypt.__about__.__version__,
# which bcrypt>=4.1 removed — pin below that to avoid a runtime
# AttributeError the first time a password is hashed or verified.
bcrypt==4.0.1
python-multipart==0.0.20

# --- Observability ---
structlog==24.4.0
sentry-sdk[fastapi]==2.19.2

# --- Utilities ---
httpx==0.28.1
tenacity==9.0.0
orjson==3.10.12
python-dotenv==1.0.1
